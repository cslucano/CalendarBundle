{% extends 'SgCalendarBundle::layout.html.twig' %}
{% form_theme remove_form 'SgCalendarBundle:Form:theme.html.twig' %}

{% block title %}{{ 'calendar.calendar.action.remove.title'|trans({}, 'messages') }}{% endblock %}

{% block header %}

    <h2>{{ 'calendar.calendar.action.remove.title'|trans({}, 'messages') }}</h2>

    <hr />

{% endblock %}

{% block sidebar %}
{% endblock %}

{% block content %}

    {% set confirmMsg = 'calendar.calendar.action.remove.confirm'|trans({}, 'messages') %}

    {{ form_start(remove_form, {'attr': {'class': 'form-horizontal well'}, 'action': path('sg_calendar_delete_calendar', { 'id': entity.id }) }) }}
        {{ 'calendar.entity.calendar.name'|trans({}, 'messages') }}: {{ entity.name }}
        {{ form_widget(remove_form) }}
        <div class="form-actions">
            <button formnovalidate="formnovalidate" class="btn btn-danger" type="submit" onclick="if(!confirm('{{ confirmMsg }}')) { return false; }">
                {{ 'calendar.calendar.action.remove.submit'|trans({}, 'messages') }}
            </button>
        </div>
    {{ form_end(remove_form) }}

{% endblock %}